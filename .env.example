# Optional but recommended for higher GitHub API limits
GITHUB_TOKEN=

# Optional AI enrichment; without it the pipeline uses evidence-only fallback summaries
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna

# Optional social sources
X_BEARER_TOKEN=
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_IDS=
